jQuery(document).ready(function($) {
  console.log("okay");
  });

$('#caratname_field').change( function() {
   // code will goes here 
  })


// copy js from function.php



/* 
    function caretvalue() {
        var productweight = document.getElementById("_weight").value;
        var pergram = document.getElementById("caratname_field").value;

        if(productweight < 1) {
            alert('Product weight is empty. Add product weight first under shipping Tab');
        }else{
            var result = productweight * pergram;
            document.getElementById("_regular_price").value = result;
        }
    }

 */

